# Base URL of the Express backend (server/).
# For Expo Go on a physical phone, use the LAN IP of the machine running the
# server, not localhost — e.g. http://192.168.1.20:5000
EXPO_PUBLIC_API_URL=http://192.168.1.20:5000
