---
phase: 02-expo-native-client
plan: 04
type: qa
wave: 4
depends_on: ["02-03"]
model_delegation:
  orchestrator: "Claude Sonnet — QA analysis, bug triage, fix verification"
  glm: "bug fixes for UI/layout issues"
  kimi: "bug fixes for data/logic issues"
autonomous: false
requirements: [FE-01, FE-02, FE-03, DOCS-01, SCH-01, OPS-01]

# This plan is a QA pass, not feature development.
# Orchestrator drives: reads bug list → delegates fixes → verifies → marks done.

must_haves:
  truths:
    - "App loads to login screen in < 3s cold start on a physical iOS or Android device via Expo Go"
    - "Login → Documents → tap document → back: no crashes, no white screens"
    - "Scan a real document photo: AI extraction returns a result within 15s, not a timeout"
    - "Mark a task complete: list updates within 1s (optimistic update), no stale UI after refresh"
    - "Kill app and reopen: session persists, user lands on Documents tab (not login)"
    - "School tab: select a child, tasks render; deselect, all children render"
    - "No TypeScript errors in `mobile/` — `npx tsc --noEmit` exits 0"
    - "No ESLint errors — `npx eslint mobile/` exits 0"
    - "Expo Go QR share link works for a second device (family member phone)"
    - "Settings screen exists with: user email display, sign out button — both functional"
    - "All screens have a loading state AND an error state (not blank)"
    - "No hardcoded test data, no Hassan references, no console.log in production paths"
  artifacts:
    - path: "mobile/app/(app)/settings/index.tsx"
      provides: "Settings screen with sign out"
      contains: "signOut"
    - path: "02-04-BUG-LOG.md"
      provides: "QA bug log: found + fixed"
      contains: "Status: FIXED"

qa_checklist:
  device_testing:
    - "Install Expo Go on iPhone (iOS 16+)"
    - "Install Expo Go on Android (API 30+)"
    - "Run `npx expo start --tunnel` in mobile/ (tunnel for remote device testing)"
    - "Scan QR on both devices"
    - "Complete full flow: register → login → scan doc → view → complete task"
  regression_matrix:
    - screen: "Login"
      cases: ["valid login", "wrong password", "empty fields", "network offline"]
    - screen: "Register"  
      cases: ["valid register", "existing email", "weak password"]
    - screen: "Documents"
      cases: ["empty state", "list with items", "tap to detail", "pull to refresh", "add button"]
    - screen: "Document Detail"
      cases: ["all fields present", "no expiry date (null)", "delete document"]
    - screen: "Scan"
      cases: ["camera capture", "file picker PDF", "file picker image", "extraction failure"]
    - screen: "School"
      cases: ["no children", "child with tasks", "child with no tasks", "task complete"]
    - screen: "Actions"
      cases: ["empty", "filter all", "filter overdue", "filter this week", "complete task"]
    - screen: "Settings"
      cases: ["displays email", "sign out works"]

tasks:
  - id: "02-04-T1"
    title: "Add Settings screen"
    autonomous: true
    model: "glm"
    instructions: |
      Write `mobile/app/(app)/settings/index.tsx`:
        - Show logged-in user email (from useSession)
        - App version (from app.json via expo-constants)
        - Sign out button → useSession.signOut()
        - Add Settings tab to (app)/_layout.tsx (gear icon, 4th tab)
    verify: "Sign out from Settings clears session, lands on Login"

  - id: "02-04-T2"
    title: "QA run — device testing"
    autonomous: false
    checkpoint: human-action
    instructions: |
      Operator must:
      1. Run `cd mobile && npx expo start --tunnel`
      2. Scan QR on a physical device (iOS or Android)
      3. Walk through regression_matrix above
      4. Document each failure in `02-04-BUG-LOG.md` format:
         ```
         ## Bug [N]: [screen] — [description]
         Steps: ...
         Expected: ...
         Actual: ...
         Status: OPEN
         ```
      5. Reply with: `qa-done: N-bugs-found`

  - id: "02-04-T3"
    title: "Fix QA bugs"
    autonomous: true
    depends_on: ["02-04-T2"]
    model_routing: |
      Read 02-04-BUG-LOG.md. For each OPEN bug:
      - UI/layout bugs → delegate to ask_glm
      - Data/logic/auth bugs → delegate to ask_kimi
      - Architectural issues → orchestrator (Claude Sonnet) fixes directly
      Mark each bug FIXED in the log after applying fix.
      Commit each fix atomically: "fix(02-04): [screen] — [description]"
    verify: "All bugs in 02-04-BUG-LOG.md show Status: FIXED"

  - id: "02-04-T4"
    title: "Expo Go distribution prep"
    autonomous: true
    model: "orchestrator"
    depends_on: ["02-04-T3"]
    instructions: |
      1. Verify `npx tsc --noEmit` exits 0 in mobile/
      2. Verify no `console.log` in production paths (grep -r "console.log" mobile/app mobile/lib mobile/components)
      3. Update `mobile/.env.example` with all required EXPO_PUBLIC_* vars
      4. Write `mobile/TESTING.md`:
         - How to install Expo Go
         - How to get the QR code from the tester (tunnel URL)
         - What to test (link to regression_matrix)
         - How to report bugs (WhatsApp/Telegram message format)
      5. Tag git: `git tag v0.1-expo-go-beta`
    verify: "mobile/TESTING.md exists, git tag exists"

---

## Phase 02 Done When

All must_haves are TRUE, 02-04-BUG-LOG.md has zero OPEN bugs, and a family member can install Expo Go, scan the QR, and complete the full document scan + task complete flow without assistance.

## Graphify Update

After this plan, run: `/graphify mobile/ --update`
This adds the Expo codebase to the knowledge graph for Phase 03 planning.
