---
phase: 02-expo-native-client
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
model_delegation:
  glm: "DocumentCard, ExpiryBadge, scan/upload UI, list screens"
  kimi: "API hooks, data fetch logic, upload flow, error states"
files_modified:
  - mobile/lib/api/documents.ts
  - mobile/app/(app)/documents/index.tsx
  - mobile/app/(app)/documents/[id].tsx
  - mobile/app/(app)/documents/scan.tsx
  - mobile/components/DocumentCard.tsx
  - mobile/components/ExpiryBadge.tsx
  - mobile/components/ui/EmptyState.tsx
  - mobile/components/ui/LoadingSpinner.tsx
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-07]

must_haves:
  truths:
    - "Documents list screen fetches from `GET /api/documents` with JWT, renders DocumentCard per item"
    - "DocumentCard shows: title, category badge, expiry date colored by urgency (red <7d, amber <30d, green otherwise)"
    - "Tapping DocumentCard navigates to `documents/[id]` showing full detail: title, category, expiry, assigned member, notes"
    - "Scan screen lets user pick a file (expo-document-picker) OR take a photo (expo-camera)"
    - "After pick/photo, scan screen POSTs base64 to `POST /api/documents/scan` with JWT, shows AI-extracted result for confirmation"
    - "User confirms or edits extracted fields, then saves via `POST /api/documents`"
    - "Empty state renders when no documents exist (not a blank screen)"
    - "Loading spinner renders while fetching (not a blank screen)"
    - "Network error shows inline error message with retry button"
    - "All API calls include `Authorization: Bearer <session.access_token>` header"
    - "`lib/api/documents.ts` exports typed functions: `listDocuments()`, `getDocument(id)`, `createDocument(data)`, `scanDocument(base64, mimeType)`"
  artifacts:
    - path: "mobile/lib/api/documents.ts"
      provides: "Typed document API client"
      contains: "listDocuments"
    - path: "mobile/app/(app)/documents/index.tsx"
      provides: "Document list screen"
      contains: "DocumentCard"
    - path: "mobile/app/(app)/documents/[id].tsx"
      provides: "Document detail screen"
      contains: "ExpiryBadge"
    - path: "mobile/app/(app)/documents/scan.tsx"
      provides: "Scan/upload screen"
      contains: "expo-document-picker"
    - path: "mobile/components/DocumentCard.tsx"
      provides: "Document list item"
      contains: "ExpiryBadge"
    - path: "mobile/components/ExpiryBadge.tsx"
      provides: "Color-coded expiry indicator"
      contains: "daysUntilExpiry"

tasks:
  - id: "02-02-T1"
    title: "Document API client"
    autonomous: true
    model: "kimi"
    instructions: |
      Write `mobile/lib/api/documents.ts`:
        Base URL: process.env.EXPO_PUBLIC_API_URL (defaults to Vercel deployment URL)
        All functions accept no auth arg — read from supabase.auth.getSession() internally
        Types:
          Document { id, title, category, expiry_date, assigned_member_id, notes, file_url, created_at }
          ScanResult { title, category, expiry_date, assigned_member_id, confidence, raw_text }
        Functions:
          listDocuments(): Promise<Document[]> — GET /api/documents
          getDocument(id: string): Promise<Document> — GET /api/documents/[id]
          createDocument(data: Omit<Document,'id'|'created_at'>): Promise<Document> — POST /api/documents
          updateDocument(id: string, data: Partial<Document>): Promise<Document> — PATCH /api/documents/[id]
          deleteDocument(id: string): Promise<void> — DELETE /api/documents/[id]
          scanDocument(base64: string, mimeType: string): Promise<ScanResult> — POST /api/documents/scan
        Error handling: throw with { message, status } shape
    verify: "TypeScript compiles, no any types"

  - id: "02-02-T2"
    title: "ExpiryBadge + DocumentCard components"
    autonomous: true
    model: "glm"
    depends_on: ["02-02-T1"]
    instructions: |
      Write `mobile/components/ExpiryBadge.tsx`:
        Props: { expiry_date: string | null }
        Logic: daysUntil = diff from today. <0 = EXPIRED (red), <7 = red, <30 = amber, else green
        Renders: colored pill with text (e.g. "7 days", "EXPIRED", "3 months")
      Write `mobile/components/DocumentCard.tsx`:
        Props: { document: Document, onPress: () => void }
        Layout: title (bold), category chip, ExpiryBadge, assigned member
        Style: Surface card (#0d1226), border #2a3555, tap highlight
    verify: "Component renders in Expo Go without crash"

  - id: "02-02-T3"
    title: "Documents list + detail screens"
    autonomous: true
    model: "glm"
    depends_on: ["02-02-T2"]
    instructions: |
      Write `mobile/app/(app)/documents/index.tsx`:
        - useEffect: call listDocuments() on mount, store in state
        - FlatList of DocumentCard → navigate to documents/[id] on press
        - Header right: "+" button → navigate to documents/scan
        - Loading: show LoadingSpinner
        - Empty: show EmptyState "No documents yet. Tap + to scan your first."
        - Error: inline error + retry button
      Write `mobile/app/(app)/documents/[id].tsx`:
        - useLocalSearchParams for id
        - Fetch getDocument(id) on mount
        - Render all fields: title, category, expiry (ExpiryBadge), assigned member, notes
        - Delete button with confirmation alert
        - Loading/error states
    verify: "List loads from live API, tapping navigates to detail"

  - id: "02-02-T4"
    title: "Scan/upload screen"
    autonomous: true
    model: "kimi"
    depends_on: ["02-02-T3"]
    instructions: |
      Write `mobile/app/(app)/documents/scan.tsx`:
        Step 1 — Pick source:
          Two buttons: "Take Photo" (expo-camera) | "Choose File" (expo-document-picker)
          Camera: capture → get base64 → proceed to Step 2
          File picker: accept PDF + images → get base64 → proceed to Step 2
        Step 2 — AI extraction:
          Show "Analyzing..." spinner
          Call scanDocument(base64, mimeType)
          On success: show editable form pre-filled with ScanResult fields
          On error: show error + "Try again" button
        Step 3 — Confirm + save:
          User can edit title, category (picker), expiry date (date picker), assigned member
          "Save" → createDocument(data) → navigate back to documents list
          Loading state on save button
    verify: "Full scan → confirm → save flow works in Expo Go"
---

## Checkpoint

After T4: pick a real document photo, confirm AI extraction, save, verify it appears in the documents list.
