---
phase: 02-expo-native-client
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
model_delegation:
  glm: "TaskCard, ChildBadge, school/actions screen layouts"
  kimi: "task API client, school extraction hook, actions filter logic"
files_modified:
  - mobile/lib/api/tasks.ts
  - mobile/lib/api/school.ts
  - mobile/app/(app)/school/index.tsx
  - mobile/app/(app)/school/[childId].tsx
  - mobile/app/(app)/actions/index.tsx
  - mobile/components/TaskCard.tsx
  - mobile/components/ChildBadge.tsx
  - mobile/components/ChildSelector.tsx
autonomous: true
requirements: [SCH-01, SCH-02, SCH-03, SCH-04, TG-01]

must_haves:
  truths:
    - "School screen shows per-child task list; tapping a child renders their open tasks ordered by due_date ASC"
    - "TaskCard shows: title, due_date badge (same urgency color as ExpiryBadge), priority chip, source link"
    - "Task can be marked complete via swipe or button — calls PATCH /api/tasks/[id] { completed_at: now() }"
    - "Actions screen shows ALL pending tasks + reminders across all children, ordered by due_date"
    - "Actions screen has filter chips: All / Overdue / This Week / By Child"
    - "ChildBadge renders child name + grade in a colored pill; color is deterministic from child id"
    - "School screen empty state: 'No school tasks. Upload a school communication to get started.'"
    - "Actions screen empty state: 'All caught up! No pending actions.'"
    - "All API calls include Authorization header from session"
    - "`lib/api/tasks.ts` exports: `listTasks(filters?)`, `getTask(id)`, `completeTask(id)`, `createTask(data)`, `deleteTask(id)`"
    - "`lib/api/school.ts` exports: `listChildren()`, `getChildTasks(childId)`, `addSchoolComm(base64, childId?)`"
  artifacts:
    - path: "mobile/lib/api/tasks.ts"
      provides: "Tasks API client"
      contains: "listTasks"
    - path: "mobile/lib/api/school.ts"
      provides: "School + children API client"
      contains: "listChildren"
    - path: "mobile/app/(app)/school/index.tsx"
      provides: "School hub screen"
      contains: "ChildBadge"
    - path: "mobile/app/(app)/actions/index.tsx"
      provides: "Actions/reminders screen"
      contains: "TaskCard"
    - path: "mobile/components/TaskCard.tsx"
      provides: "Task list item"
      contains: "completeTask"
    - path: "mobile/components/ChildBadge.tsx"
      provides: "Child identifier pill"
      contains: "childId"

tasks:
  - id: "02-03-T1"
    title: "Tasks + School API clients"
    autonomous: true
    model: "kimi"
    instructions: |
      Write `mobile/lib/api/tasks.ts`:
        Types:
          Task { id, title, due_date, priority: 'high'|'medium'|'low', child_id?, completed_at?, source_doc_id?, family_id }
          TaskFilters { childId?: string, overdue?: boolean, thisWeek?: boolean }
        Functions:
          listTasks(filters?: TaskFilters): Promise<Task[]> — GET /api/tasks with query params
          getTask(id): Promise<Task> — GET /api/tasks/[id]
          completeTask(id): Promise<Task> — PATCH /api/tasks/[id] { completed_at: new Date().toISOString() }
          createTask(data): Promise<Task> — POST /api/tasks
          deleteTask(id): Promise<void> — DELETE /api/tasks/[id]
      Write `mobile/lib/api/school.ts`:
        Types:
          Child { id, name, grade, aliases: string[], family_id }
        Functions:
          listChildren(): Promise<Child[]> — GET /api/children
          getChildTasks(childId): Promise<Task[]> — GET /api/tasks?childId=X
          addSchoolComm(base64: string, childId?: string): Promise<Task[]> — POST /api/school/extract
            Returns array of extracted tasks for confirmation
    verify: "TypeScript compiles, no any"

  - id: "02-03-T2"
    title: "TaskCard + ChildBadge + ChildSelector"
    autonomous: true
    model: "glm"
    depends_on: ["02-03-T1"]
    instructions: |
      Write `mobile/components/ChildBadge.tsx`:
        Props: { child: Child }
        Color: deterministic from child.id (hash → pick from 5 accent colors)
        Renders: "[name] • Grade [grade]" pill
      Write `mobile/components/TaskCard.tsx`:
        Props: { task: Task, onComplete: () => void, onPress?: () => void }
        Layout: title, due_date badge (ExpiryBadge reused), priority chip (red/amber/green), ChildBadge if child_id
        Swipe right to complete: react-native-gesture-handler swipeable → calls onComplete
        Completed state: strikethrough title, muted colors
      Write `mobile/components/ChildSelector.tsx`:
        Props: { children: Child[], selected: string | null, onSelect: (id: string | null) => void }
        Renders horizontal scroll of ChildBadge chips; tapping toggles selection (null = All)
    verify: "Components render in Expo Go"

  - id: "02-03-T3"
    title: "School Hub + Actions screens"
    autonomous: true
    model: "glm"
    depends_on: ["02-03-T2"]
    instructions: |
      Write `mobile/app/(app)/school/index.tsx`:
        - Fetch listChildren() on mount
        - ChildSelector at top
        - When child selected: fetch getChildTasks(childId), render FlatList of TaskCard
        - When no child selected: show all children as tappable cards with open task count badge
        - Empty state per child: "No tasks for [name]. Upload school comm to get started."
        - FAB: "+" → expo-document-picker → pick school comm → addSchoolComm() → show extracted tasks modal for confirm → createTask for each confirmed
      Write `mobile/app/(app)/actions/index.tsx`:
        - Fetch listTasks() on mount
        - Filter chips: All / Overdue / This Week / By Child (ChildSelector inline)
        - FlatList of TaskCard with onComplete calling completeTask(id) → optimistically remove from list
        - Section headers: "Overdue" (red), "This Week", "Later"
        - Empty state: "All caught up!"
    verify: "Both screens load data from live API, complete task removes it from list"
---

## Checkpoint

After T3: mark a task complete on the Actions screen, verify it disappears from list and shows as complete on refresh.
School screen: select a child, verify their tasks appear.
