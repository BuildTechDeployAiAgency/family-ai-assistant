---
phase: 01-security-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CLAUDE.md
  - .gitignore
  - .env.example
  - .github/dependabot.yml
  - package.json
  - server/database.db
  - dist/
  - .chrome-profile/
autonomous: false
requirements: [SEC-01, SEC-02, SEC-08]
user_setup:
  - service: supabase
    why: "Hosted Postgres + Auth + RLS (D-01, D-06). Required before any schema/RLS work in Plan 02."
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> anon public"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> service_role (SECRET)"
    dashboard_config:
      - task: "Create new Supabase project (free tier OK for POC)"
        location: "https://supabase.com/dashboard"
      - task: "Auth -> Providers -> Email: enable, set Confirm email = ON, password min length 12"
        location: "Supabase Dashboard -> Authentication -> Providers -> Email"
  - service: vercel
    why: "Host static Vite frontend + serverless functions under api/ (D-02)."
    env_vars:
      - name: (all SUPABASE_*, OPENROUTER_API_KEY, UPSTASH_* set in Plan 03)
        source: "Vercel Dashboard -> Project -> Settings -> Environment Variables"
    dashboard_config:
      - task: "Create Vercel project linked to GitHub repo BuildTechDeployAiAgency/family-ai-assistant"
        location: "https://vercel.com/new"
  - service: upstash
    why: "Serverless-friendly Redis for @upstash/ratelimit on /api/ai/* (Plan 03, D-15)."
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash Console -> Database -> REST API -> UPSTASH_REDIS_REST_URL"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash Console -> Database -> REST API -> UPSTASH_REDIS_REST_TOKEN"
    dashboard_config:
      - task: "Create Upstash Redis database (Global, free tier)"
        location: "https://console.upstash.com/"
  - service: openrouter
    why: "Rotate the previously-committed key as hygiene (D-15 specifics, RESEARCH Open Q #3)."
    dashboard_config:
      - task: "Revoke key sk-or-v1-a731f1e7741... and issue a new one"
        location: "https://openrouter.ai/keys"

must_haves:
  truths:
    - "REQUIREMENTS.md, ROADMAP.md, CLAUDE.md describe Supabase+Vercel stack — no SQLite/Fly.io/Express/JWT references remain in Phase 1 scope"
    - "SEC-09 is marked OBSOLETE in REQUIREMENTS.md (replaced by env-validated Supabase keys, per D-11)"
    - "`.env.example` exists at repo root listing every required env var with placeholder values"
    - "`.gitignore` covers `.env*` (except `.env.example`), `*.db`, `dist`, `.chrome-profile` (verify; already present)"
    - "`server/database.db`, `dist/`, and `.chrome-profile/` directories no longer exist on disk"
    - "package.json has express, sqlite3, bcryptjs, jsonwebtoken, cors REMOVED and @supabase/supabase-js, envalid, zod, pino, @upstash/ratelimit, @upstash/redis ADDED with exact-pinned versions"
    - "Supabase project, Vercel project, Upstash Redis database exist and credentials are recorded by the operator"
    - "OpenRouter key has been rotated; new key is recorded for Plan 03 env wiring"
    - "Hassan family seed data has been moved out of `src/App.jsx` into `src/fixtures/hassan.js` and is imported only behind `import.meta.env.DEV`"
    - "Production bundle (`npm run build`) contains no `Hassan` substring and no `sk-or-` substring — verified by grep gate"
    - ".github/dependabot.yml exists enabling weekly npm dependency scanning"
  artifacts:
    - path: ".env.example"
      provides: "Required env var contract"
      contains: "SUPABASE_URL\nSUPABASE_ANON_KEY\nSUPABASE_SERVICE_ROLE_KEY\nOPENROUTER_API_KEY\nUPSTASH_REDIS_REST_URL\nUPSTASH_REDIS_REST_TOKEN"
    - path: ".github/dependabot.yml"
      provides: "Weekly npm dependency scanning per CONCERNS 'no dependency scanning'"
      contains: "package-ecosystem: \"npm\""
    - path: "src/fixtures/hassan.js"
      provides: "Hassan demo data, DEV-only import"
      min_lines: 50
    - path: "package.json"
      provides: "Dependency manifest after stack pivot"
      contains: "@supabase/supabase-js"
  key_links:
    - from: "src/App.jsx"
      to: "src/fixtures/hassan.js"
      via: "import gated by import.meta.env.DEV"
      pattern: "import.meta.env.DEV"
    - from: ".env.example"
      to: ".gitignore"
      via: "`!.env.example` negation pattern allows the template through while .env* is blocked"
      pattern: "!.env.example"
---

<objective>
Reconcile project documentation with the Supabase+Vercel pivot (D-01..D-16), provision the three external services Phases 02 and 03 depend on, scrub leaked-secret risk, swap dependency manifests, and remove Hassan seed data from the production bundle.

Purpose: Plans 02 and 03 cannot start without Supabase/Vercel/Upstash credentials and a clean dependency tree. Docs must match reality before any executor reads them.

Output: Updated REQUIREMENTS.md/ROADMAP.md/CLAUDE.md, populated `.env.example`, swapped `package.json`, deleted stale runtime state, moved Hassan fixtures, rotated OpenRouter key, three external service accounts ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-backend-foundation/01-CONTEXT.md
@.planning/phases/01-security-backend-foundation/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@.gitignore
@package.json
@src/App.jsx

<interfaces>
<!-- Current `.env` (gitignored, never published) per CONTEXT code_context: -->
<!-- VITE_OPENROUTER_API_KEY=sk-or-v1-a731f1e7741... (DROP VITE_ prefix; rotate) -->
<!-- VITE_OPENROUTER_MODEL=google/gemini-3.5-flash (wrong slug; Phase 3 owns) -->
<!-- JWT_SECRET=...  (REMOVE; no custom JWT under Supabase Auth) -->
<!-- PORT=5000       (REMOVE; no Express server) -->

<!-- New required env set per D-11 + RESEARCH lib/env.js pattern: -->
<!-- SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY -->
<!-- OPENROUTER_API_KEY (server-side, no VITE_ prefix) -->
<!-- UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN -->
<!-- (Deferred to later phases: GOOGLE_CLIENT_SECRET in Phase 3, TELEGRAM_BOT_TOKEN in Phase 4) -->

<!-- Hassan seed data location in current code: src/App.jsx:17-378 (constants INITIAL_DOCUMENTS, INITIAL_EMAILS, MOCK_AI_RESPONSES, DEFAULT_RENEWAL_PLANS) -->
<!-- Existing .gitignore already covers .env*, *.db, dist, .chrome-profile — verify and patch only if drift. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile upstream docs with Supabase+Vercel pivot</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, CLAUDE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full)
    - .planning/ROADMAP.md (full, focus Phase 1 success criteria)
    - CLAUDE.md (full, focus Constraints section)
    - .planning/phases/01-security-backend-foundation/01-CONTEXT.md (D-01..D-16, "Upstream Doc Drift" section)
  </read_first>
  <action>
    Per CONTEXT D-03 (mandatory doc-update task before any code change), edit three files:

    1. `.planning/REQUIREMENTS.md`:
       - Strike SEC-09 by appending `~~SEC-09: JWT secret fallback removed~~ — OBSOLETE under Supabase Auth pivot (D-11). Replaced by SEC-01 expansion to cover SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY.` Keep the line numbered for traceability; do not delete it.
       - Update SEC-01 description to list the new required env set: `OPENROUTER_API_KEY`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`. Note `GOOGLE_CLIENT_SECRET` deferred to Phase 3 and `TELEGRAM_BOT_TOKEN` deferred to Phase 4.
       - Rewrite DATA-01..DATA-11 wording: `DATA-04` becomes `family_id NOT NULL FK + Postgres RLS policy gating family membership`. `DATA-05` becomes `Thin lib/db/{resource}.js wrappers around @supabase/supabase-js; RLS is the security boundary, not WHERE family_id = ? predicates` (per D-09). `DATA-06` becomes `Supabase CLI migrations under supabase/migrations/` (per D-05). `DATA-11` becomes `Vercel serverless functions under api/ replace server.js; shared code under lib/` (per RESEARCH "Recommended Project Structure").
       - In the "Out of Scope" table, strike `Postgres migration in v1` row with a `~~...~~` strike-through + footnote `Reversed by Phase 1 CONTEXT D-01 (full Supabase pivot).`
    2. `.planning/ROADMAP.md`:
       - Rewrite Phase 1 Success Criteria #2 to read about Supabase keys env-validated (drop `JWT_SECRET`, `OPENROUTER_API_KEY`, `GOOGLE_CLIENT_SECRET`, `TELEGRAM_BOT_TOKEN` Express framing). Keep `OPENROUTER_API_KEY` as required at boot. Add `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`.
       - Rewrite Phase 1 Success Criteria #5 to drop `server.js is decomposed into adapters/rest.js, routes/, middleware/, db/, services/` and replace with `Vercel serverless functions under api/ replace Express; shared code under lib/{env,supabase,schemas,db,logger,ratelimit}; helmet replaced by vercel.json headers; CORS unnecessary for same-origin browser->/api/* (allowlist remains for external webhooks); zod validates each function entry; pino structured logs with PII redaction; @upstash/ratelimit on /api/ai/*.`
       - Update Phase 4 Success Criteria #5: strike `Fly.io app deployed with count=1, persistent volume, WAL-mode SQLite, healthcheck, Litestream backups` and replace with `Vercel project deployed (frontend + api/ functions), Supabase project healthy, Upstash Redis configured, .env.example matches deployed env, manual restore drill from Supabase point-in-time recovery documented`. (Phase 4 still owns deployment ops; this is alignment only.)
    3. `CLAUDE.md` (the project-root CLAUDE.md, not the workspace-root one):
       - In the `### Constraints` block, change `**Tech stack**: Vite + React 19, Express 5, SQLite` to `**Tech stack**: Vite + React 19 (frontend), Vercel serverless functions under api/ (backend), Supabase Postgres + Auth + RLS, Upstash Redis for rate-limiting`.
       - Change `**Hosting**: Vercel (frontend) + Fly.io (backend + SQLite volume)` to `**Hosting**: Vercel (frontend + api/ serverless functions), Supabase (managed Postgres + Auth), Upstash (Redis)`.
       - Change `**Auth**: email/password (no OAuth providers in v1)` to `**Auth**: Supabase Auth, email/password (no OAuth providers in v1)`.

    Implementing decisions: D-01, D-02, D-03, D-05, D-09, D-11. Implements requirement SEC-01 (scope expansion).
  </action>
  <verify>
    <automated>grep -c "Supabase" .planning/REQUIREMENTS.md | awk '$1 >= 5 { exit 0 } { exit 1 }' && grep -c "Fly.io" .planning/ROADMAP.md | awk '$1 == 0 { exit 0 } { exit 1 }' && grep -c "Vercel serverless" CLAUDE.md | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -q "OBSOLETE" .planning/REQUIREMENTS.md && grep -q "Supabase Auth" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md SEC-09 line contains `OBSOLETE` and references D-11
    - REQUIREMENTS.md SEC-01 lists the 6 Phase-1-required env vars
    - REQUIREMENTS.md DATA-04/05/06/11 wording matches D-05/D-07/D-09 reinterpretation
    - REQUIREMENTS.md "Out of Scope" Postgres row is struck-through with D-01 footnote
    - ROADMAP.md Phase 1 Success Criteria #2 lists Supabase/Upstash keys (no JWT_SECRET)
    - ROADMAP.md Phase 1 Success Criteria #5 references api/ + lib/ (no server.js/adapters/routes Express layout)
    - ROADMAP.md Phase 4 Success Criteria #5 has no `Fly.io` or `Litestream` references
    - CLAUDE.md Constraints lists Supabase + Vercel + Upstash (no SQLite, no Fly.io, no Express in tech stack)
    - `git diff --stat` shows all three files modified
  </acceptance_criteria>
  <done>All three doc files reflect the Supabase+Vercel topology. Downstream plans 02 and 03 can be read without contradiction.</done>
</task>

<task type="auto">
  <name>Task 2: Swap dependency manifest, delete stale runtime state, wire .env.example + dependabot, move Hassan fixtures</name>
  <files>package.json, package-lock.json, .env.example, .gitignore, .github/dependabot.yml, src/fixtures/hassan.js, src/App.jsx, server/database.db (delete), dist/ (delete), .chrome-profile/ (delete)</files>
  <read_first>
    - package.json (full)
    - .gitignore (full — verify .env*, *.db, dist, .chrome-profile already present)
    - src/App.jsx (lines 1-400 — Hassan seed data + REFERENCE_DATE constant)
    - .planning/phases/01-security-backend-foundation/01-RESEARCH.md (sections "Standard Stack" installation block, "Runtime State Inventory", and "Concerns -> Resolution Map" rows for `.chrome-profile`, dist, *.db)
  </read_first>
  <action>
    Five sub-steps, sequential within this task:

    (a) Dependency swap (per RESEARCH Standard Stack + Concerns map "Bleeding-edge versions" pin requirement):
        - `npm uninstall express sqlite3 bcryptjs jsonwebtoken cors`
        - `npm install --save-exact @supabase/supabase-js@2.106.1 envalid@8.1.1 zod@4.4.3 pino@10.3.1 @upstash/ratelimit@2.0.8 @upstash/redis@1.38.0`
        - `npm install --save-exact --save-dev supabase@2.100.1 pino-pretty`
        - Drop `concurrently` only if no remaining `npm run dev` consumer needs it; KEEP it for now (Plan 03 may use `vercel dev` standalone — remove in Plan 03 if confirmed unused).
        - Verify `package.json` no longer lists express/sqlite3/bcryptjs/jsonwebtoken/cors and DOES list the six new runtime packages with exact versions (no `^` prefix).

    (b) Create `.env.example` at repo root with exactly these keys (placeholders only, NEVER real values):
        ```
        # Supabase (D-06, D-11)
        SUPABASE_URL=https://<project-ref>.supabase.co
        SUPABASE_ANON_KEY=eyJ... (anon public key from dashboard)
        SUPABASE_SERVICE_ROLE_KEY=eyJ... (SECRET — server-side only, never VITE_-prefixed)

        # OpenRouter (D-15, server-side only)
        OPENROUTER_API_KEY=sk-or-v1-...

        # Upstash Redis for @upstash/ratelimit (D-15)
        UPSTASH_REDIS_REST_URL=https://<region>-<id>.upstash.io
        UPSTASH_REDIS_REST_TOKEN=...

        # Frontend Supabase client needs only the anon key (browser-safe). Vite reads VITE_*.
        VITE_SUPABASE_URL=${SUPABASE_URL}
        VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}

        # Deferred to later phases:
        # GOOGLE_CLIENT_SECRET=...      (Phase 3 — Drive OAuth)
        # TELEGRAM_BOT_TOKEN=...        (Phase 4 — Telegram bot)
        ```
        Add a top-of-file comment: `# Template — copy to .env (gitignored) and fill in real values. Required at boot per envalid (lib/env.js).`

    (c) Verify `.gitignore` (per RESEARCH Concerns map: already covers all four). If any of `.env*`, `*.db`, `dist`, `.chrome-profile` is missing, add it. The negation `!.env.example` MUST be present so the template is committable. Run `git check-ignore -v .env .env.local server/database.db dist .chrome-profile` and confirm each is reported as ignored.

    (d) Create `.github/dependabot.yml` enabling weekly npm scanning (per CONCERNS "No npm audit / dependency scanning"):
        ```
        version: 2
        updates:
          - package-ecosystem: "npm"
            directory: "/"
            schedule:
              interval: "weekly"
            open-pull-requests-limit: 5
        ```

    (e) Move Hassan seed data + delete stale runtime state (per D-16, SEC-08, RESEARCH Runtime State Inventory):
        - Create `src/fixtures/hassan.js`. Cut the constants `FAMILY_MEMBERS`, `INITIAL_DOCUMENTS`, `INITIAL_EMAILS`, `MOCK_AI_RESPONSES`, `DEFAULT_RENEWAL_PLANS` (and any other `Hassan`-string-bearing constants) from `src/App.jsx` (lines roughly 17-378 — verify exact range by reading). Export each as a named export. Match existing JSX no-semicolon style (per CLAUDE.md project conventions).
        - In `src/App.jsx`, replace the constants with a DEV-gated dynamic import. Pattern: at the top of the file under existing imports, add a side-effect-free conditional:
          `let HASSAN = { FAMILY_MEMBERS: [], INITIAL_DOCUMENTS: [], INITIAL_EMAILS: [], MOCK_AI_RESPONSES: {}, DEFAULT_RENEWAL_PLANS: {} }`
          `if (import.meta.env.DEV) { HASSAN = await import('./fixtures/hassan.js') }` — adjust syntax to React-compatible top-level (use a dynamic import inside a `useEffect` if top-level await unavailable, or use a synchronous `import.meta.env.DEV ? require-equivalent`). Acceptable alternative: static `import * as HASSAN from './fixtures/hassan.js'` plus runtime gate `const FAMILY_MEMBERS = import.meta.env.DEV ? HASSAN.FAMILY_MEMBERS : []` — Vite tree-shakes the import in prod builds when the gate is statically false. Choose whichever path lets the grep gate in Verify pass.
        - References inside the component body (`FAMILY_MEMBERS`, `INITIAL_DOCUMENTS`, etc.) become the local DEV-gated bindings — behavior unchanged in dev, empty in prod.
        - Delete the local file `server/database.db` (per D-04). Delete `dist/` (per RESEARCH Runtime State row: contains baked-in VITE_OPENROUTER_API_KEY). Delete the entire `.chrome-profile/` directory (per CONCERNS "Dead Code"). All three are gitignored already so `git status` will not flag the deletions.

    Implementing decisions: D-04, D-16. Implements requirements SEC-01 (env contract), SEC-02 (gitignore verification), SEC-08 (Hassan removed from prod bundle).
  </action>
  <verify>
    <automated>test -f .env.example && grep -q "SUPABASE_SERVICE_ROLE_KEY" .env.example && grep -q "OPENROUTER_API_KEY" .env.example && test -f .github/dependabot.yml && test -f src/fixtures/hassan.js && ! test -f server/database.db && ! test -d dist && ! test -d .chrome-profile && ! grep -q '"express"' package.json && ! grep -q '"sqlite3"' package.json && ! grep -q '"bcryptjs"' package.json && ! grep -q '"jsonwebtoken"' package.json && grep -q '"@supabase/supabase-js": "2.106.1"' package.json && grep -q '"envalid": "8.1.1"' package.json && grep -q '"zod": "4.4.3"' package.json && grep -q '"pino": "10.3.1"' package.json && grep -q '"@upstash/ratelimit": "2.0.8"' package.json && grep -q '"@upstash/redis": "1.38.0"' package.json && grep -v '^#' src/App.jsx | grep -c "INITIAL_DOCUMENTS = \[" | awk '$1 == 0 { exit 0 } { exit 1 }' && npm run build && ! grep -r "sk-or-" dist/ && ! grep -r "Hassan" dist/</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` exists with all 6 required Phase 1 env vars (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, OPENROUTER_API_KEY, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN) plus the two VITE_SUPABASE_* mirrors and deferred-phase comments
    - `.github/dependabot.yml` exists, valid YAML, `package-ecosystem: "npm"`
    - `package.json` no longer lists express/sqlite3/bcryptjs/jsonwebtoken/cors and lists the six new packages with exact versions (no `^`)
    - `src/fixtures/hassan.js` exists exporting the moved constants
    - `src/App.jsx` no longer declares the Hassan constant arrays inline (grep shows zero non-comment matches for `INITIAL_DOCUMENTS = [`)
    - `server/database.db`, `dist/`, `.chrome-profile/` no longer exist on disk
    - `npm run build` succeeds
    - `grep -r "sk-or-" dist/` and `grep -r "Hassan" dist/` both return zero matches (SEC-03 + SEC-08 acceptance gate per RESEARCH Pitfall 7)
    - `git check-ignore` reports `.env`, `server/database.db`, `dist`, `.chrome-profile` as ignored
  </acceptance_criteria>
  <done>Dependency tree reflects the new stack. Production bundle has no Hassan strings and no `sk-or-` substrings. Stale runtime state is gone. Doc-driven env contract exists.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Operator provisions Supabase + Vercel + Upstash and rotates OpenRouter key</name>
  <what-built>
    Tasks 1 and 2 reconciled docs and dependencies. Three external accounts and a key rotation now require human-only actions (account signups, dashboard config, manual key rotation) — Claude cannot complete these via CLI/API for the first-time setup.
  </what-built>
  <how-to-verify>
    Operator performs (with order matters):

    1. **Supabase project** — Create new project at https://supabase.com/dashboard (free tier). After project is ready:
       - Project Settings -> API: copy `Project URL` -> record as `SUPABASE_URL`
       - Project Settings -> API -> Project API keys: copy `anon` `public` -> record as `SUPABASE_ANON_KEY`
       - Project Settings -> API -> Project API keys: copy `service_role` `secret` -> record as `SUPABASE_SERVICE_ROLE_KEY` (NEVER paste into client code)
       - Authentication -> Providers -> Email: enable; set `Confirm email` ON; set `Minimum password length` to 12 (resolves CONCERNS "no password strength requirements")
       - Authentication -> Rate Limits: note the current per-IP login rate (default ~30 req/5min per IP per RESEARCH Pitfall 5 / A1). Record the actual current value — Plan 03 uses it to decide whether `/api/auth/*` needs Upstash on top.

    2. **Vercel project** — Sign in at https://vercel.com/new. Import the GitHub repo `BuildTechDeployAiAgency/family-ai-assistant`. Framework preset = `Vite` (Vercel auto-detects). Do not deploy yet (Plan 03 lands `api/` functions and `vercel.json`). Record the assigned project name + production URL placeholder.

    3. **Upstash Redis** — Create database at https://console.upstash.com/ (Global, free tier). REST API tab:
       - Copy `UPSTASH_REDIS_REST_URL`
       - Copy `UPSTASH_REDIS_REST_TOKEN`

    4. **OpenRouter key rotation** (per RESEARCH Open Q #3) — At https://openrouter.ai/keys:
       - Revoke the existing key `sk-or-v1-a731f1e7741...`
       - Create a new key with name `family-ai-assistant-v1`
       - Record as `OPENROUTER_API_KEY` (server-side use only, NO `VITE_` prefix)

    5. **Local `.env` file** — Copy `.env.example` to `.env` and fill in the six real values recorded above. Do not commit (`.gitignore` already protects).

    6. **Verify Docker is running** (per RESEARCH Environment Availability A3 / Pitfall list). Run `docker info` in a terminal — must succeed. Required for `supabase start` in Plan 02. If Docker is not installed, install Docker Desktop now.

    Confirm completion by replying with: `provisioned: supabase=<project-ref>, vercel=<project-name>, upstash=<db-name>, openrouter-rotated=YES, docker=running` — Plan 02 cannot start until this confirmation arrives.
  </how-to-verify>
  <resume-signal>
    Reply with the `provisioned: ...` line above (or describe any blocker). Plans 02 and 03 will execute once confirmation received.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local FS -> git remote | Risk of committing secrets if .gitignore drifts |
| Operator -> external dashboards | First-time account creation; service-role key must never leave the operator's clipboard for client code |
| .env.example -> client bundle | Vite inlines any `VITE_*` var into the public bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | `.env` accidentally committed | mitigate | `.gitignore` covers `.env*` with `!.env.example` negation; `git check-ignore` verified in Task 2; rotated OpenRouter key in Task 3 limits blast radius even on past leak |
| T-01-02 | Information disclosure | Stale `dist/` shipping old VITE_OPENROUTER_API_KEY | mitigate | Task 2 deletes `dist/` and `npm run build` + grep gate verifies no `sk-or-` substring (RESEARCH Pitfall 7) |
| T-01-03 | Information disclosure | Hassan PII in production bundle | mitigate | Task 2 moves fixtures to `src/fixtures/hassan.js` behind `import.meta.env.DEV`; grep gate on `dist/` for `Hassan` (SEC-08) |
| T-01-04 | Elevation of privilege | Service-role key VITE_-prefixed and leaked to browser | mitigate | `.env.example` explicitly comments `NEVER VITE_-prefixed` and lists it under server-only; Plan 03 adds CI grep gate on `VITE_*SERVICE*`/`VITE_*SECRET*` (RESEARCH Pitfall 3) |
| T-01-05 | Tampering | Supply chain — newly installed deps | mitigate | All six packages passed slopcheck [OK] per RESEARCH Package Legitimacy Audit; exact-pinned versions; dependabot enabled in Task 2 |
| T-01-SC | Tampering | npm install of new packages (@supabase/*, envalid, zod, pino, @upstash/*) | mitigate | Slopcheck audit table in RESEARCH (lines 168-178) — all 9 packages [OK]. No `[ASSUMED]` or `[SUS]` packages; legitimacy blocking-human checkpoint not required for this set |
| T-01-06 | Spoofing | Operator pastes credentials into wrong field during Task 3 | accept | Documentation in Task 3 explicit; ops error is residual risk acceptable for one-person POC |

</threat_model>

<verification>
- `git diff` shows REQUIREMENTS.md, ROADMAP.md, CLAUDE.md modifications consistent with D-01..D-16
- `package.json` matches new stack; `npm install` completes cleanly; `package-lock.json` regenerated
- `npm run build` succeeds; `grep -r "sk-or-" dist/` and `grep -r "Hassan" dist/` both empty
- `git check-ignore -v .env server/database.db dist .chrome-profile` reports all four ignored
- Operator confirmation message received in Task 3
</verification>

<success_criteria>
- All doc drift items in CONTEXT.md "Upstream Doc Drift" section resolved
- `.env.example` is the single source of truth for required env vars
- Dependency manifest reflects the new stack with exact-pinned versions
- Zero secrets and zero Hassan data shipped in `dist/`
- Supabase, Vercel, Upstash accounts ready; OpenRouter key rotated; Docker verified for Plan 02
</success_criteria>

<output>
Create `.planning/phases/01-security-backend-foundation/01-01-SUMMARY.md` when done.
</output>
