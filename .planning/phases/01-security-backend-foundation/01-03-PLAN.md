---
phase: 01-security-backend-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - vercel.json
  - scripts/verify-01-03-task-1.sh
  - lib/logger.js
  - lib/ratelimit.js
  - lib/schemas/ai.js
  - lib/schemas/documents.js
  - lib/schemas/emails.js
  - lib/schemas/tasks.js
  - lib/auth.js
  - api/health.js
  - api/ai/complete.js
  - api/documents/index.js
  - api/documents/[id].js
  - api/emails/index.js
  - api/emails/sync.js
  - api/tasks/index.js
  - api/tasks/[id].js
  - src/lib/supabase.js
  - src/App.jsx
  - server/ (delete entire directory)
  - vite.config.js
  - package.json
autonomous: false
requirements: [SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, DATA-07, DATA-08, DATA-09, DATA-10, DATA-11]

must_haves:
  truths:
    - "`/api/health` returns 200 with `{ ok: true }` from a deployed Vercel preview; importing `lib/env.js` with a missing key returns 500 (proves SEC-01 fail-fast)"
    - "OpenRouter calls go through `POST /api/ai/complete` using server-side `OPENROUTER_API_KEY`; production `dist/` contains no `sk-or-` substring (SEC-03 grep gate)"
    - "Every serverless function under `api/*.js` validates its request body with a zod schema from `lib/schemas/*` before any DB call (SEC-06)"
    - "Every function attaches PII-redacting `pino` logger from `lib/logger.js` (SEC-07)"
    - "`vercel.json` declares HSTS, X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy, CSP via `headers` config (SEC-04 — replaces `helmet`)"
    - "`@upstash/ratelimit` is applied at `/api/ai/complete` (per-family identifier); native Supabase Auth throttling documented as covering login per RESEARCH A1/Pitfall 5 (SEC-05)"
    - "Frontend (`src/App.jsx`) uses Supabase Auth via `src/lib/supabase.js` for login/register/logout; no fetch to `/api/auth/*`; no custom JWT in code; no `localStorage.family_jwt_token` writes"
    - "`server/` directory is deleted; `npm run dev` is replaced/augmented by `vercel dev`; `vite.config.js` proxy to `localhost:5000` is removed"
    - "DATA-07..DATA-10 ownership is documented in 01-03-SUMMARY.md as deferred to Phase 3 (drive_connections, llm_audit_log) and Phase 4 (telegram_chats, reminders) per RESEARCH Open Q #5 — Phase 1 closes the DATA-11 decomposition with the api/ + lib/ layout"
    - "After Vercel deploy, operator-led smoke test confirms register → login → create document → list document → AI proxy call all succeed end-to-end"
  artifacts:
    - path: "vercel.json"
      provides: "Security headers + function config"
      contains: "Strict-Transport-Security"
    - path: "api/ai/complete.js"
      provides: "Server-side OpenRouter proxy (SEC-03)"
      contains: "OPENROUTER_API_KEY"
    - path: "api/health.js"
      provides: "Env validation surface (RESEARCH Pitfall 2 mitigation)"
      contains: "import { env }"
    - path: "lib/logger.js"
      provides: "pino logger with PII redaction"
      contains: "redact"
    - path: "lib/ratelimit.js"
      provides: "Upstash rate-limit factory for /api/ai/*"
      contains: "Ratelimit"
    - path: "src/lib/supabase.js"
      provides: "Browser Supabase client (anon key only)"
      contains: "VITE_SUPABASE_ANON_KEY"
  key_links:
    - from: "api/*"
      to: "lib/env.js"
      via: "module-load import triggers envalid validation"
      pattern: "from '\\.\\./.+/env\\.js'"
    - from: "api/ai/complete.js"
      to: "lib/ratelimit.js"
      via: "aiLimit.limit(`family:${familyId}`) gate before OpenRouter call"
      pattern: "aiLimit\\.limit"
    - from: "src/App.jsx"
      to: "src/lib/supabase.js"
      via: "Supabase Auth replaces custom /api/auth/* fetch + localStorage JWT"
      pattern: "supabase\\.auth"
---

<objective>
Replace `server/server.js` with Vercel serverless functions under `api/`, harden every entry point (env validation, zod, pino, security headers, rate limiting), proxy OpenRouter server-side, swap the frontend's custom auth for Supabase Auth.

Purpose: Closes every Phase 1 SEC-* requirement and DATA-11 (decomposition). After this plan, Phase 2 frontend modularization and Phase 3 LLM router work on a hardened, RLS-backed, rate-limited foundation.

Output: Working `api/` directory replacing Express, hardened shared libs (logger/ratelimit/schemas/auth), Supabase-Auth frontend, deleted Express server, deployed Vercel preview with smoke-tested end-to-end flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-backend-foundation/01-CONTEXT.md
@.planning/phases/01-security-backend-foundation/01-RESEARCH.md
@.planning/phases/01-security-backend-foundation/01-01-SUMMARY.md
@.planning/phases/01-security-backend-foundation/01-02-SUMMARY.md
@server/server.js
@server/middleware.js
@src/App.jsx
@vite.config.js
@lib/env.js
@lib/supabase.js
@lib/db/documents.js
@lib/db/emails.js
@lib/db/tasks.js

<interfaces>
<!-- Vercel function shape (RESEARCH Pattern 3): -->
<!-- export default async function handler(req, res) -->
<!-- req.headers.authorization -> 'Bearer <jwt>' -->
<!-- res.status(n).json(body) | res.json(body) -->

<!-- Routes carried over from server/server.js, translated 1:1: -->
<!--   POST /api/auth/login       -> REMOVED (Supabase Auth direct from browser per D-06) -->
<!--   POST /api/auth/register    -> REMOVED -->
<!--   GET  /api/auth/me          -> REMOVED (supabase.auth.getUser() in browser) -->
<!--   GET  /api/documents        -> api/documents/index.js GET   -->
<!--   POST /api/documents        -> api/documents/index.js POST  -->
<!--   PUT  /api/documents/:id    -> api/documents/[id].js PUT    -->
<!--   DELETE /api/documents/:id  -> api/documents/[id].js DELETE -->
<!--   GET  /api/emails           -> api/emails/index.js GET     -->
<!--   POST /api/emails/sync      -> api/emails/sync.js POST     -->
<!--   GET  /api/tasks            -> api/tasks/index.js GET      -->
<!--   POST /api/tasks            -> api/tasks/index.js POST     -->
<!--   PUT  /api/tasks/:id        -> api/tasks/[id].js PUT       -->
<!--   DELETE /api/tasks/:id      -> api/tasks/[id].js DELETE    -->
<!--   NEW: POST /api/ai/complete -> api/ai/complete.js POST     -->
<!--   NEW: GET  /api/health      -> api/health.js GET           -->

<!-- Frontend auth migration (D-06): -->
<!--   Replace localStorage 'family_jwt_token' with supabase.auth session (default localStorage key managed by supabase-js per RESEARCH Open Q #1) -->
<!--   Replace fetch('/api/auth/login') with supabase.auth.signInWithPassword({ email, password }) -->
<!--   Replace fetch('/api/auth/register') with supabase.auth.signUp({ email, password }) + post-confirm flow -->
<!--   Replace Authorization: Bearer ${token} construction with supabase.auth.getSession() -> session.access_token -->

<!-- Rate limit per RESEARCH Pattern 5 + Pitfall 5: -->
<!--   /api/ai/complete: aiLimit = slidingWindow(20, '1 m') keyed by family:${familyId} -->
<!--   /api/auth/*: NONE (Supabase Auth native throttle documented; planner verified in 01-01 Task 3 step 1) -->

<!-- DATA-07..10 explicitly deferred per RESEARCH Open Q #5: -->
<!--   drive_connections, llm_audit_log -> Phase 3 owns -->
<!--   telegram_chats, reminders        -> Phase 4 owns -->
<!--   Phase 1 closes DATA-11 (decomposition) with the api/ + lib/ structure ONLY. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship vercel.json, lib/logger.js, lib/ratelimit.js, lib/schemas/*, lib/auth.js, and api/health.js + api/ai/complete.js</name>
  <files>vercel.json, lib/logger.js, lib/ratelimit.js, lib/schemas/ai.js, lib/schemas/documents.js, lib/schemas/emails.js, lib/schemas/tasks.js, lib/auth.js, api/health.js, api/ai/complete.js, src/lib/supabase.js, package.json</files>
  <read_first>
    - .planning/phases/01-security-backend-foundation/01-RESEARCH.md (sections "Pattern 3: Function Entry Validation + Logging", "Pattern 5: Upstash Rate Limit", "Pattern 6: Pino with PII/Secret Redaction", "Pattern 7: vercel.json Security Headers", "Pitfall 1: Zod v4 Breaking Changes", "Pitfall 2: Cold Start Env Throws Silently")
    - lib/env.js, lib/supabase.js (from Plan 01-02)
    - server/server.js (existing /api/auth/* handlers as reference for what NOT to port — Supabase Auth handles login direct from browser)
  </read_first>
  <action>
    Files to create (no fenced code in this action — patterns are quoted in RESEARCH):

    (a) `vercel.json` per RESEARCH Pattern 7. `headers` block applied to `source: "/(.*)"` with: Strict-Transport-Security (`max-age=63072000; includeSubDomains; preload`), X-Content-Type-Options (`nosniff`), X-Frame-Options (`DENY`), Referrer-Policy (`strict-origin-when-cross-origin`), Permissions-Policy (`camera=(), microphone=(), geolocation=()`), Content-Security-Policy starting in REPORT-ONLY mode for first deploy (per RESEARCH note: `Content-Security-Policy-Report-Only` first; promote to enforcing once dev console is clean). CSP `connect-src 'self' https://*.supabase.co https://openrouter.ai`. Add `functions` block with `maxDuration: 10` on `api/ai/complete.js` (LLM calls may take time; default is 10s on hobby).

    (b) `lib/logger.js` per RESEARCH Pattern 6 — `pino` instance, log level from `LOG_LEVEL` env (default `info`), `redact.paths` covering: `req.headers.authorization`, `req.headers.cookie`, `*.password`, `*.passwordHash`, `*.apiKey`, `*.token`, `*.access_token`, `*.refresh_token`, `*.OPENROUTER_API_KEY`, `*.SUPABASE_SERVICE_ROLE_KEY`. Censor `[REDACTED]`. Export `logger` singleton.

    (c) `lib/ratelimit.js` per RESEARCH Pattern 5 — Upstash Redis client from `env.UPSTASH_REDIS_REST_URL` + `env.UPSTASH_REDIS_REST_TOKEN`. Export `aiLimit = new Ratelimit({ redis, limiter: Ratelimit.slidingWindow(20, '1 m'), analytics: true })`. Inline comment: `// SEC-05: /api/auth/* relies on Supabase Auth native throttle (~30 req/5min per IP, verified in Plan 01-01 Task 3 step 1).`

    (d) `lib/schemas/*.js` — Per resource, zod v4 schemas (per RESEARCH Pitfall 1: use `.min(1)` not `.nonempty()`; `parsed.error.issues` is the v4 error shape):
       - `lib/schemas/ai.js`: `aiCompleteSchema` — `{ messages: array(object({ role: enum(['system','user','assistant']), content: string().min(1) })), model: string().optional(), max_tokens: number().int().positive().max(8000).optional(), temperature: number().min(0).max(2).optional() }`
       - `lib/schemas/documents.js`: `documentCreateSchema` (title required, family_id required uuid, category/document_number/notes optional, expiry_date date-string optional, member_id uuid optional), `documentUpdateSchema` (partial of create).
       - `lib/schemas/emails.js`: `emailUpsertSchema` (single email shape), `emailSyncSchema` (array of `emailUpsertSchema`, max 200 items to align with Vercel body limit).
       - `lib/schemas/tasks.js`: `taskCreateSchema`, `taskUpdateSchema`.

    (e) `lib/auth.js` — helper `requireUser(req)` that extracts JWT from `Authorization: Bearer <jwt>` header, calls `userClient(jwt).auth.getUser()`, returns `{ user, sb }` or throws `{ status: 401, error: 'unauthorized' }`. Used by every authenticated handler.

    (f) `api/health.js` — Imports `env` from `lib/env.js` (RESEARCH Pitfall 2: this makes env validation surface via healthcheck instead of in user requests). Returns `res.status(200).json({ ok: true, ts: new Date().toISOString() })`. No auth required.

    (g) `api/ai/complete.js` — RESEARCH Pattern 3 + 5:
       1. Import `env`, `logger`, `aiLimit`, `requireUser`, `aiCompleteSchema`.
       2. Reject non-POST with 405.
       3. `const { user, sb } = await requireUser(req)` — 401 if no JWT.
       4. `aiCompleteSchema.safeParse(req.body)` — 400 if invalid.
       5. Lookup user's family_id via `sb.from('family_members').select('family_id').single()`.
       6. `const { success } = await aiLimit.limit(`family:${familyId}`)` — 429 if exceeded.
       7. `fetch('https://openrouter.ai/api/v1/chat/completions', { headers: { Authorization: `Bearer ${env.OPENROUTER_API_KEY}` }, body: JSON.stringify({ ...parsed.data, model: parsed.data.model || 'google/gemini-2.5-flash' }) })` — note placeholder default model per CONTEXT specifics; Phase 3 LLM-08 owns final model selection.
       8. Return OpenRouter response body verbatim. Log latency + status via `logger.info({ status, latencyMs, familyId })`.

    (h) `src/lib/supabase.js` — Browser-side Supabase client using `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` from `import.meta.env`. Export `supabase` singleton. NEVER use the service-role key (per RESEARCH Pitfall 3 — Vite would inline it).

    (i) `package.json` — add `"vercel-dev": "vercel dev"` script. (Don't replace `dev` yet; Task 3 handles full server/ cutover.)

    Implementing decisions: D-01 (no custom auth in code), D-09 (supabase-js direct), D-12 (vercel.json headers), D-13 (zod), D-14 (pino), D-15 (Upstash). Implements SEC-03, SEC-04, SEC-05, SEC-06, SEC-07.
  </action>
  <verify>
    <automated>bash scripts/verify-01-03-task-1.sh</automated>
    <script_to_create>
      Create `scripts/verify-01-03-task-1.sh` with `set -euo pipefail` and one labeled check per line. Each check echoes its label, runs the test, and exits non-zero on failure. Required checks (in order, each its own line that exits on failure):
        1. echo "[1/22] vercel.json exists";        test -f vercel.json
        2. echo "[2/22] HSTS header";                grep -q "Strict-Transport-Security" vercel.json
        3. echo "[3/22] CSP header";                 grep -q "Content-Security-Policy" vercel.json
        4. echo "[4/22] X-Frame-Options";            grep -q "X-Frame-Options" vercel.json
        5. echo "[5/22] X-Content-Type-Options";     grep -q "X-Content-Type-Options" vercel.json
        6. echo "[6/22] Referrer-Policy";            grep -q "Referrer-Policy" vercel.json
        7. echo "[7/22] Permissions-Policy";         grep -q "Permissions-Policy" vercel.json
        8. echo "[8/22] lib/logger.js exists";       test -f lib/logger.js
        9. echo "[9/22] pino redact paths";          grep -q "redact" lib/logger.js && grep -q "authorization" lib/logger.js
       10. echo "[10/22] lib/ratelimit.js exists";   test -f lib/ratelimit.js && grep -q "slidingWindow" lib/ratelimit.js
       11. echo "[11/22] zod schemas: ai";           test -f lib/schemas/ai.js
       12. echo "[12/22] zod schemas: documents";    test -f lib/schemas/documents.js
       13. echo "[13/22] zod schemas: emails";       test -f lib/schemas/emails.js
       14. echo "[14/22] zod schemas: tasks";        test -f lib/schemas/tasks.js
       15. echo "[15/22] lib/auth.js requireUser";   test -f lib/auth.js && grep -q "getUser" lib/auth.js
       16. echo "[16/22] api/health.js imports env"; test -f api/health.js && grep -q "import { env }" api/health.js
       17. echo "[17/22] api/ai/complete.js";        test -f api/ai/complete.js
       18. echo "[18/22] AI proxy uses server key";  grep -q "OPENROUTER_API_KEY" api/ai/complete.js
       19. echo "[19/22] AI proxy rate-limit gate";  grep -q "aiLimit.limit" api/ai/complete.js
       20. echo "[20/22] src/lib/supabase.js anon";  test -f src/lib/supabase.js && grep -q "VITE_SUPABASE_ANON_KEY" src/lib/supabase.js
       21. echo "[21/22] no SERVICE_ROLE in src/lib/supabase.js"; ! grep -q "SERVICE_ROLE" src/lib/supabase.js
       22. echo "[22/22] zod v4 syntax (no nonempty)"; ! grep -q "nonempty()" lib/schemas/ai.js
      End with: `echo "Task 1 verification: ALL PASS"`. `chmod +x scripts/verify-01-03-task-1.sh` after creation.
    </script_to_create>
  </verify>
  <acceptance_criteria>
    - `vercel.json` declares all 6 security headers + CSP (report-only or enforcing) + functions.maxDuration override for ai/complete
    - `lib/logger.js` has redact paths covering authorization/cookie/password/token/apiKey/service-role
    - `lib/ratelimit.js` exports `aiLimit` with slidingWindow(20, '1 m')
    - 4 zod schema files exist using zod v4 syntax (`.min(1)` not `.nonempty()`)
    - `lib/auth.js` extracts JWT, calls `getUser`, returns `{ user, sb }`
    - `api/health.js` imports env (forces validation on cold start per RESEARCH Pitfall 2)
    - `api/ai/complete.js` gates on rate-limit before OpenRouter fetch, uses server-side OPENROUTER_API_KEY
    - `src/lib/supabase.js` uses only `VITE_SUPABASE_ANON_KEY`; no `SERVICE_ROLE` substring anywhere in `src/`
    - `package.json` has `vercel-dev` script
  </acceptance_criteria>
  <done>Hardened entry-point primitives ready. AI proxy live (locally). Browser Supabase client wired.</done>
</task>

<task type="auto">
  <name>Task 2a: Ship api/* CRUD routes (documents/emails/tasks) with zod validation</name>
  <files>api/documents/index.js, api/documents/[id].js, api/emails/index.js, api/emails/sync.js, api/tasks/index.js, api/tasks/[id].js</files>
  <read_first>
    - server/server.js (full — translate each /api/documents, /api/emails, /api/tasks handler 1:1)
    - lib/auth.js, lib/db/documents.js, lib/db/emails.js, lib/db/tasks.js, lib/schemas/* (from Task 1 + Plan 01-02)
  </read_first>
  <action>
    Six API route files — each follows the RESEARCH Pattern 3 template:
       - Import `env`, `logger`, `requireUser`, the relevant `lib/db/*` functions, and the relevant `lib/schemas/*`.
       - Method dispatch (GET/POST/PUT/DELETE); 405 for unhandled.
       - `requireUser(req)` gate (401 on failure).
       - `parsed = schema.safeParse(req.body)` for write paths (400 on invalid). Map snake_case ↔ camelCase per CLAUDE.md convention (`server/server.js:121-140,230-237` is the existing precedent).
       - Call the corresponding `lib/db/*` function with the user's `sb`.
       - On error: `logger.error({ err })`, return 500 with generic `{ error: 'internal' }` — no stack traces to client.
       - On success: 200/201 + JSON body. NO `setHeader` for CORS (same-origin browser→/api/* per D-12).

       Specific files:
       - `api/documents/index.js` — GET (list via `listDocuments(sb)`), POST (validate + `createDocument(sb, payload)`, 201)
       - `api/documents/[id].js` — GET (`getDocument(sb, id)`), PUT (validate patch + `updateDocument(sb, id, patch)`), DELETE (`deleteDocument(sb, id)`, 204)
       - `api/emails/index.js` — GET (`listEmails(sb)`)
       - `api/emails/sync.js` — POST (validate array via `emailSyncSchema` + `upsertEmailsBulk(sb, payloads)` — single-round-trip replaces sequential `INSERT OR REPLACE` per CONCERNS perf row)
       - `api/tasks/index.js` — GET / POST
       - `api/tasks/[id].js` — PUT / DELETE

    Implementing decisions: D-09, D-13. Implements SEC-06 (zod on every write), SEC-07 (pino on every handler), DATA-11 partial (api/ shell — Task 2b finalizes by deleting server/).
  </action>
  <verify>
    <automated>test -f api/documents/index.js && test -f api/documents/'[id]'.js && test -f api/emails/index.js && test -f api/emails/sync.js && test -f api/tasks/index.js && test -f api/tasks/'[id]'.js && for f in api/documents/index.js api/documents/'[id]'.js api/emails/index.js api/emails/sync.js api/tasks/index.js api/tasks/'[id]'.js; do grep -q "requireUser" "$f" || { echo "missing requireUser in $f"; exit 1; }; done && for f in api/documents/index.js api/emails/sync.js api/tasks/index.js; do grep -q "safeParse" "$f" || { echo "missing zod safeParse in $f"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - All 6 route files exist under `api/`
    - Every file calls `requireUser`
    - Every write path (POST/PUT) calls `safeParse`
    - Error path returns generic `{ error: 'internal' }`, never stack traces
  </acceptance_criteria>
  <done>Serverless CRUD layer ready. Routes are auth-gated, zod-validated, pino-logged, RLS-backed.</done>
</task>

<task type="auto">
  <name>Task 2b: Swap App.jsx auth to Supabase Auth, delete server/, rewire dev tooling, run build + grep gates</name>
  <files>src/App.jsx, vite.config.js, server/ (delete), package.json</files>
  <read_first>
    - server/middleware.js (authenticateToken — REPLACED by lib/auth.js requireUser; do not port)
    - src/App.jsx (lines covering: API key state, callOpenRouter ~line 987, login/register/logout, fetch('/api/auth/*'), Authorization header construction, family_jwt_token localStorage usage)
    - src/lib/supabase.js (from Task 1)
    - vite.config.js (proxy to :5000)
    - api/* files from Task 2a (do not re-read in full — already in context)
  </read_first>
  <action>
    Two sub-steps:

    (a) Swap `src/App.jsx` auth to Supabase Auth (per D-06 + RESEARCH "Concerns -> Resolution Map" rows for JWT/localStorage/logout):
       - Add `import { supabase } from './lib/supabase.js'` near top.
       - Replace existing `fetch('/api/auth/login', ...)` calls with `supabase.auth.signInWithPassword({ email, password })`.
       - Replace `fetch('/api/auth/register', ...)` with `supabase.auth.signUp({ email, password, options: { data: { display_name } } })`. Document in the catch path: confirm-email flow is REQUIRED (Plan 01-01 Task 3 set Supabase project "Confirm email" = ON).
       - Replace `verifyToken` startup logic with `supabase.auth.getSession()` + `supabase.auth.onAuthStateChange(...)` subscription (per RESEARCH Concerns map "Auth useEffect dependency churn").
       - Replace `localStorage.setItem('family_jwt_token', ...)` and `localStorage.removeItem('family_jwt_token')` calls with no-ops — Supabase manages session storage. Keep `family_*` namespace cleanup on logout (per CONCERNS "Token leak via incomplete localStorage cleanup"): iterate `Object.keys(localStorage)` and remove anything starting with `family_` BEFORE calling `supabase.auth.signOut()`.
       - All authenticated `fetch('/api/...', { headers: { Authorization: 'Bearer ' + token } })` calls remain — but `token` is now sourced from `(await supabase.auth.getSession()).data.session?.access_token`.
       - `callOpenRouter` (~line 987 in App.jsx): swap direct `fetch('https://openrouter.ai/...')` for `fetch('/api/ai/complete', { method: 'POST', headers: { Authorization: 'Bearer ' + token, 'Content-Type': 'application/json' }, body: JSON.stringify({ messages, model, max_tokens, temperature }) })`. DELETE references to `VITE_OPENROUTER_API_KEY` and any `apiKey` state holding an OpenRouter key (per SEC-03). The settings UI for "OpenRouter API key" disappears.
       - DELETE the Lockscreen component's custom `/api/auth/me` polling — Supabase `onAuthStateChange` replaces it.

       Conventions to preserve (per CLAUDE.md / CONVENTIONS.md): `src/App.jsx` omits trailing semicolons in JSX — match existing style; 2-space indent, single quotes.

    (b) Cleanup + dev tooling:
       - DELETE the entire `server/` directory (`server.js`, `db.js`, `middleware.js`).
       - DELETE the `proxy` block in `vite.config.js` (no more `localhost:5000`).
       - REMOVE the `server` script and the `node server/server.js` portion of `dev` in `package.json`. Replace `dev` with `concurrently "vite" "vercel dev --listen 5000 --no-open"` so the local DX stays "one command, vite at :5173 + functions at :5000" — OR drop `concurrently` and switch to `vercel dev` alone if it serves the Vite frontend correctly (planner discretion; pick whichever works in Vercel CLI v36+). Document the chosen flow in 01-03-SUMMARY.md.
       - Uninstall now-unused `concurrently` IF the chosen DX no longer needs it.
       - Run `npm run build` and grep gates: `! grep -r "sk-or-" dist/` AND `! grep -r "Hassan" dist/` AND `! grep -rn "family_jwt_token" src/` AND `! grep -rn "openrouter\.ai" src/` (frontend no longer talks to OpenRouter directly).

    Implementing decisions: D-01, D-02, D-06. Implements SEC-03 (frontend cutover) and closes DATA-11 (api/ + lib/ replace server.js).
  </action>
  <verify>
    <automated>! test -d server && ! grep -q "localhost:5000" vite.config.js && ! grep -rn "family_jwt_token" src/ && ! grep -rn "openrouter\.ai" src/ && ! grep -rn "VITE_OPENROUTER_API_KEY" src/ && grep -rn "supabase.auth.signInWithPassword" src/ && grep -rn "/api/ai/complete" src/ && ! grep -q "node server/server.js" package.json && npm run build && ! grep -r "sk-or-" dist/ && ! grep -r "Hassan" dist/</automated>
  </verify>
  <acceptance_criteria>
    - `server/` directory deleted; `vite.config.js` proxy removed
    - `src/` contains zero references to `family_jwt_token`, `openrouter.ai`, `VITE_OPENROUTER_API_KEY`
    - `src/` contains `supabase.auth.signInWithPassword` and `/api/ai/complete`
    - `package.json` no longer references `node server/server.js`
    - `npm run build` succeeds; production `dist/` has no `sk-or-` and no `Hassan` substrings (SEC-03 + SEC-08 final gate)
  </acceptance_criteria>
  <done>Express server gone. Frontend speaks Supabase Auth + uses the AI proxy. Build verified clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator deploys to Vercel preview and runs the end-to-end smoke test</name>
  <what-built>
    Task 1 shipped the hardened serverless primitives + AI proxy. Task 2 ported every route, swapped the frontend to Supabase Auth, and deleted Express. The phase is code-complete locally; the operator must now (1) deploy to Vercel preview, (2) confirm the env validation fail-fast works, and (3) smoke-test the full register → login → CRUD → AI proxy flow.
  </what-built>
  <how-to-verify>
    Operator runs these steps in order and pastes results:

    1. **Push to Vercel preview** — `git add -A && git commit -m "phase 1: serverless cutover" && git push`. Vercel auto-deploys the preview branch. Watch the dashboard.

    2. **Set env vars in Vercel** (Project -> Settings -> Environment Variables — Production + Preview + Development scopes):
       - `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (the service-role MUST NOT be VITE_-prefixed)
       - `OPENROUTER_API_KEY` (the rotated key from Plan 01-01 Task 3)
       - `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`
       - `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` (mirrors for the browser bundle — anon key only)
       - Redeploy after setting envs.

    3. **Env fail-fast check** (RESEARCH Pitfall 2):
       - Temporarily remove `UPSTASH_REDIS_REST_TOKEN` in Vercel envs and redeploy.
       - `curl https://<preview-url>/api/health` MUST return 500 within seconds (cold start envalid throw).
       - Restore the env var, redeploy, confirm `curl https://<preview-url>/api/health` returns `{ ok: true, ts: ... }`.

    4. **SEC-03 production grep gate** — On your machine: `curl -s https://<preview-url>/assets/index-*.js | head -c 200000 | grep -c "sk-or-"` MUST return `0`. Same for `Hassan`. (Vercel-served bundle, not local dist.)

    5. **End-to-end smoke test** via the deployed preview UI:
       a. Register a new test account (real email — confirmation required).
       b. Confirm via email link.
       c. Log in. Confirm the dashboard loads.
       d. Manually run this once via `supabase` SQL editor: `insert into public.families (name) values ('Smoke Test Family') returning id;` then `insert into public.family_members (family_id, user_id, display_name) values ('<family-id>', '<your-auth-uid>', 'Tester');` — this is the manual onboarding stand-in (OPS-01 first-run flow is Phase 4).
       e. Create a document via the UI. Reload — document persists.
       f. Trigger any AI-backed action in the UI (e.g., scan/analyze flow that calls callOpenRouter). Confirm a response comes back. Check Vercel function logs: `pino` entry for `/api/ai/complete` shows status + latency + familyId, NO `sk-or-` in logs, NO `Authorization` header value in logs.
       g. Hit the AI endpoint 21 times within a minute (or use a small curl loop). The 21st MUST return 429 (rate-limited per `aiLimit`).

    6. **Cross-family RLS spot-check** — Register a second test account in a different browser. Confirm. Do the manual family/family_member insert with a DIFFERENT family_id. Confirm in browser 2's UI: the first account's document is NOT visible.

    Report results in the format:
    ```
    deploy: <url>
    health: 200 with envs / 500 without envs : PASS/FAIL
    grep-sk-or in bundle: 0 : PASS/FAIL
    grep-Hassan in bundle: 0 : PASS/FAIL
    register+confirm: PASS/FAIL
    login: PASS/FAIL
    create+list document: PASS/FAIL
    ai proxy: PASS/FAIL (latency: Xms)
    rate limit 21st call: 429 : PASS/FAIL
    cross-family invisibility: PASS/FAIL
    logs scrubbed (no sk-or-, no auth header): PASS/FAIL
    ```
  </how-to-verify>
  <resume-signal>
    Reply with the smoke-test report block. Any FAIL triggers a follow-up gap-closure plan (`/gsd:plan-phase 1 --gaps`). All PASS closes Phase 1.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Vercel api/* | Same-origin; JWT in Authorization header (no cookies in v1) |
| Vercel function -> Supabase | User JWT for RLS-enforced calls; service-role bypasses RLS (none yet — Phases 3/4) |
| Vercel function -> OpenRouter | Server-side OPENROUTER_API_KEY; rate-limited per family |
| Vercel function -> Upstash Redis | Service token via HTTP; rate-limit state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | OpenRouter key in browser bundle | mitigate | Frontend `callOpenRouter` rewired to `/api/ai/complete`; CI grep gate on `dist/` for `sk-or-` (Task 2 verify + Task 3 step 4 against deployed bundle) |
| T-03-02 | DoS | Cost-runaway attack on AI proxy | mitigate | `@upstash/ratelimit` slidingWindow(20, '1 m') keyed by family per RESEARCH Pattern 5; verified by Task 3 step 5g |
| T-03-03 | Spoofing | Login brute-force | mitigate | Supabase Auth native throttle (~30 req/5min per IP per RESEARCH A1) verified by operator in 01-01 Task 3 step 1; documented in `lib/ratelimit.js` comment |
| T-03-04 | Information disclosure | Stack trace / PII in logs | mitigate | `lib/logger.js` redact paths cover authorization/cookie/password/token/apiKey/service-role per RESEARCH Pattern 6; handlers log generic `{ error: 'internal' }` to clients while pino captures the full error |
| T-03-05 | Tampering | Mass-assignment via supabase-js insert | mitigate | zod schemas validate at function entry per SEC-06; allowed-column whitelist implicit in schema |
| T-03-06 | Information disclosure | Missing security headers (clickjacking, MIME sniff, etc.) | mitigate | `vercel.json` HSTS + XCTO + XFO + Referrer-Policy + Permissions-Policy + CSP per RESEARCH Pattern 7 (SEC-04) |
| T-03-07 | Elevation of privilege | Service-role key bleeding into a user-facing route | mitigate | `api/*` files use `lib/auth.requireUser` → `userClient(jwt)`; service-role only via `serviceClient()` factory; no `api/*.js` file calls `serviceClient()` in this phase (verified by grep) |
| T-03-08 | Spoofing | CSRF on /api/* | mitigate | JWT in Authorization header (not cookie) → CSRF inapplicable per RESEARCH Known Threat Patterns; same-origin SOP |
| T-03-09 | Tampering | XSS → JWT theft from localStorage | accept | RESEARCH Open Q #1: v1 ships with Supabase default localStorage; documented tradeoff. Revisit when XSS surface grows (rich text, third-party scripts). React default escaping + CSP header reduce surface. |
| T-03-10 | Information disclosure | env var missing on Vercel → cryptic 500 | mitigate | `api/health.js` imports `env` so env validation surfaces via healthcheck not user requests (RESEARCH Pitfall 2); Task 3 step 3 explicitly verifies fail-fast |

</threat_model>

<verification>
- `npx vercel dev` boots locally; `curl localhost:5000/api/health` returns 200; with a deleted env var returns 500
- `npm run build` succeeds; `! grep -r "sk-or-" dist/` and `! grep -r "Hassan" dist/`
- Deployed Vercel preview URL responds at `/api/health`
- Operator smoke-test report shows all PASS rows
- `lib/db/*` consumed only by `api/*` (no leakage into `src/`); `serviceClient()` not called from any `api/*.js` in Phase 1
- 01-03-SUMMARY.md documents DATA-07..10 deferral to owning phases (P3: drive_connections, llm_audit_log; P4: telegram_chats, reminders) — this closes Phase 1's traceability against requirements
</verification>

<success_criteria>
- Every Phase 1 SEC requirement provably implemented and verified (grep gates + smoke test)
- DATA-11 closed by the api/ + lib/ structure replacing server.js
- DATA-07..10 explicit deferral with rationale (RESEARCH Open Q #5) — does not represent a gap since their owning phases are scheduled
- Vercel preview deploys cleanly; OpenRouter key never reaches browser; cross-family read denied
- No Express, no SQLite, no custom JWT, no bcrypt remain anywhere in the codebase
</success_criteria>

<output>
Create `.planning/phases/01-security-backend-foundation/01-03-SUMMARY.md` when done.
In the SUMMARY, explicitly call out:
- DATA-07 (drive_connections) → deferred to Phase 3 (owns Drive integration)
- DATA-08 (telegram_chats) → deferred to Phase 4 (owns Telegram bot)
- DATA-09 (reminders) → deferred to Phase 4 (owns scheduler)
- DATA-10 (llm_audit_log) → deferred to Phase 3 (owns LLM router)
- Cookie-auth decision: stayed on localStorage per RESEARCH Open Q #1
- Log sink decision: Vercel runtime logs per RESEARCH Open Q #4
</output>
